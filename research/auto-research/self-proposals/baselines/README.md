# Baselines

Use this directory for frozen score bundles that represent comparison anchors for future loops.

Examples:

- `initial-score.json` — first manually accepted baseline
- `best-score.json` — current best accepted scalar

`prepare.py` does not require a baseline file to run. Baselines are optional anchors for later comparisons.
