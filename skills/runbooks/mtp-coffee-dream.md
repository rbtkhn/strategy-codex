# Runbook — MTP with coffee and dream

Orchestration only. Methodology: [skills/mtp/SKILL.md](../mtp/SKILL.md).

WORK only; not Record.

## Sequences

| Order | When |
|-------|------|
| `coffee` → **A Confirm** → `mtp` pick **A** → `repo-hygiene-pass` | Pre-ship wedge on large dirty tree |
| `ideation-engine` → `mtp <winner>` → `pros-and-cons` | Venture filter after Top 3 |
| `dream` (quiet ok) → next `coffee` → `mtp` if dream Details flagged purpose drift | Morning after noisy day |
| `coffee` → **D** → Singularity **E. MTP orient** → one object | Thread collapse with purpose first |
| `dream` evening → `mtp` (delete-legacy / EXO step 2) | Quarterly bureaucracy audit |

## Do not

- Auto-run `mtp` inside `operator_coffee.py` or `auto_dream.py`
- Add MTP as coffee hub letter **E**
- Put MTP sentence in `last-dream.json` as policy

## Cadence voice

Many coffees, one dream. **`mtp`** is a governor invoked when speed outruns purpose — not every sip.
