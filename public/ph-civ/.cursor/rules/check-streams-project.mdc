---
description: >-
  Resolve check-streams / daily YouTube ingest via the linked skill under
  .cursor/skills/check-streams (strategy-codex).
alwaysApply: true
---

# Check streams from this workspace

`check streams` (daily roster for Davis, Diesen, Dialogue Works, Napolitano/Judging Freedom, Mercouris) is defined under **`.cursor/skills/check-streams/SKILL.md`**.

That path is a **directory junction** to **`C:\dev\strategy-codex\.cursor\skills\check-streams`** on this machine.

- **Before** running discovery, clip filtering, or raw-input materialization, **read that `SKILL.md`** end-to-end.
- **Canonical repo operations** (`codex/…`, `scripts/materialize_youtube_raw_input.py`, audits, receipts) assume workspace **`C:\dev\strategy-codex`**; switch or reference that root when executing commands.
- Single-URL work remains the separate **youtube transcript** workflow in strategy-codex.

If the junction is missing, recreate from `ph-civ` repo root:

`cmd /c mklink /J .cursor\\skills\\check-streams C:\\dev\\strategy-codex\\.cursor\\skills\\check-streams`
