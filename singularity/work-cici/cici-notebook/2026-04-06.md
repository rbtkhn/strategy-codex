# Cici notebook — Day 1

**Date:** 2026-04-06  
**Journal day:** 1 of OB1-focused learning (anchor start; adjust if your real start differs)

---

## Focus

Understand what **Open Brain** is versus a notes app: **one database**, **MCP** surface, **vector** search, **capture** from the AIs I already use. Connect that mental model to **BrewMind** later (public site + business) without mixing keys or repos.

## What I did today

- Read the **Open Brain** framing: infrastructure for persistent memory, not another siloed app.
- Located my **GitHub** home for this work: instance notes in **`xavier_self`**, upstream code in my **OB1 fork**, BrewMind site in **`brew_mind`** — three names, one story once they are wired together.
- Sketched the stack I am aiming for: **Supabase** (Postgres + pgvector) + **edge MCP** + **OpenRouter** for embeddings — matching the architecture described in my `xavier_self` README (verify against live dashboard when I log in).

## What clicked

- **Vendor-neutral** is the point: same memory whether I am in Claude, ChatGPT, or Cursor — if MCP is set up honestly, I stop re-explaining context every session.

## Friction / questions

- Still sorting **which repo** is the “source of truth” for **site** vs **brain** vs **upstream** — advisor suggests keeping BrewMind in the site repo or under `xavier_self` later; not blocking Day 1.
- Have not yet run a full **capture → search** loop end-to-end; that is the proof.

## Tomorrow (Day 2)

- Walk one **setup** subsection from my instance docs (or upstream getting-started) with a timer — **45 minutes** box — and note where I actually stopped (env, Supabase, or MCP).

## One line for my advisor

Day 1 was orientation: OB1 is the spine; BrewMind stays the public face; next is a real capture loop, not more reading.
