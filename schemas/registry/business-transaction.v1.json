{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/registry/business-transaction.v1.json",
  "title": "Business Transaction v1",
  "description": "Single financial transaction for the operator business ledger. Instance-specific (grace-mar); not template-portable.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "ts",
    "date",
    "description",
    "amount_usd",
    "type",
    "category",
    "venture"
  ],
  "properties": {
    "ts": {
      "type": "string",
      "description": "ISO 8601 timestamp of when the row was appended"
    },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Transaction date (YYYY-MM-DD)"
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "amount_usd": {
      "type": "number",
      "description": "Positive number; type field determines income vs expense"
    },
    "type": {
      "type": "string",
      "enum": [
        "income",
        "expense",
        "refund",
        "transfer"
      ]
    },
    "category": {
      "type": "string",
      "minLength": 1,
      "description": "Account category from chart-of-accounts (e.g. revenue, materials, shipping, platform_fees)"
    },
    "account": {
      "type": "string",
      "description": "Optional sub-account or payment method (e.g. etsy_payments, paypal, bank_checking)"
    },
    "tax_category": {
      "type": "string",
      "description": "Tax classification (e.g. cogs, business_expense, non_deductible, sales_tax_collected)"
    },
    "vendor_or_customer": {
      "type": "string",
      "description": "Counterparty name (vendor for expenses, customer for income)"
    },
    "receipt_ref": {
      "type": "string",
      "description": "Path or ID to receipt/invoice (e.g. etsy order number, file path)"
    },
    "venture": {
      "type": "string",
      "minLength": 1,
      "description": "Business venture slug (e.g. grace-gems)"
    },
    "notes": {
      "type": "string"
    }
  }
}
