
# Davis State Set (compat redirect)

Compatibility pointer only.

The canonical **state-set.toml** now lives at **[state-set.toml](../../voices/davis/state-set.toml)**.

Use **statecraft/voices/davis/state-set.toml** going forward.
