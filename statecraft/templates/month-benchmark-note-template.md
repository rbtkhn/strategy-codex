WORK only; not Record.

# [Month Year] benchmark note

## Purpose

Preserve `[Month Year]` as a reusable month-scale benchmark on the statecraft side so later sessions can compare against it without reopening the month as a generic backfill field.

This note is a benchmark note, not a watchlist and not a contradiction audit.

## Why [Month Year] Matters

State why the month matters at month scale and what kind of benchmark it is.

Use this section to make the maturity claim explicit:

- opening continuity/setup benchmark
- dense bridge/setup benchmark
- early mature benchmark
- another bounded benchmark class if the archive truth really supports it

## Current Month Truth

State the current month truth directly.

Include:

- what is already coherent on disk
- why the month is not currently a live closure campaign
- any important speaker-local thinness that should not be mistaken for month weakness

## Primary Benchmark Surfaces

List the existing local surfaces that make the month reusable.

Use links and one short role line per surface.

## What [Month Year] Is Good For

Preserve the month's later comparison uses.

Typical shapes:

- `[Month] vs [Month]`
- benchmark use by crisis phase
- month-shape comparison such as `dense and coherent` vs `open and underrepresented`

## What [Month Year] Still Needs

Keep this bounded and non-campaign in tone.

Allowed examples:

- guest-label normalization if later counting precision is needed
- stronger speaker-month truth surfaces if later synthesis work needs them
- repeated benchmark use before deciding whether any more capture work is justified

Do not create a missing-source queue here unless a separate contradiction object already exists.

## Use Rule

List the operator prompts this note should answer.

End with the boundary:

`Do not use this note as a watchlist, a missing-source queue, or a contradiction audit.`
