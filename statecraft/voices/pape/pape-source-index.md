WORK only; not Record.

# Pape Source Index (compat redirect)

Compatibility pointer only.

The canonical exhaustive Pape corpus route map now lives at **[pape-index.md](pape-index.md)** — all authored essays and guest appearances on disk.

Rebuild: `python3 scripts/build_pape_index.py`

Use **`pape-index.md`** going forward; this file remains only as a stable back-compat entry for older links and routing discovery.
