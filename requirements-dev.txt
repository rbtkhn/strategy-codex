# Dev/test dependencies — install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.0
pytest-mock>=3.0
pyyaml>=6.0
jsonschema>=4.0
cachetools>=5.3.0
pydantic>=2.6.0
pandas>=2.0.0

# Optional — `scripts/runtime/grace_mar_runtime_worker.py` LLM summary (not required for `--dry-run` tests)
# openai>=1.0.0
