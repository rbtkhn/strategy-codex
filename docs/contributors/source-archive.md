# Contributor checklist — source-archive

1. Verbatim captures under `source-archive/statecraft/YYYY-MM-DD/` — use statecraft source-intake skill recipe.
2. Day inventory → `day-index.md` per day (not thread-index for dated queries).
3. Authority: **source**; synthesis lives in `statecraft/daily/` and lane objects — not in archive files.
4. Entity normalization: Kiev/Kharkov policy per repo rules on operator framing.
5. Wire-verify: patch capture YAML headers only when grading falsifiers.
