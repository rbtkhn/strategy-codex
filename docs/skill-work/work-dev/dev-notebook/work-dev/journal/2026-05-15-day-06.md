# 2026-05-15 - Day 06

WORK only; not Record.

## Focus

Bernstein close on skill validation: a small invisible encoding defect can block otherwise valid doctrine from entering the runnable skill surface.

### Toscanini seam - verify before durability

- **Stance / conductor:** toscanini
- **Object:** interrupted `C,D` durability request after the Bernstein note and CM-2 receipt.
- **Seam:** interruption converts commit momentum into verification duty. The durable path is valid only after the exact write set is named, the validator is rerun, the CM-2 metadata receipt is parsed, and protected Record paths are shown clean.
- **Operational rule:** when an interrupted turn resumes into `a,b,c,d`, execute all requested movements, but put evidence before durability: inspect, verify, then commit the narrow wedge.

### Bernstein motif - invisible entry defects

- **Stance / conductor:** bernstein
- **Object:** `.cursor/skills/dream/SKILL.md` as the representative case for three BOM-frontmatter failures.
- **Motif:** invisible entry defects can break otherwise valid skill doctrine.
- **What moved:** `dream`, `fact-check`, and `gate-review-pass` each carried a UTF-8 BOM before the opening `---`. The human-readable frontmatter was intact, but `scripts/validate_skills.py` requires `text.startswith("---")`, so the doctrine failed at the byte doorway rather than at the conceptual layer.
- **Rule of thumb:** when a skill looks valid to the eye but fails metadata validation, inspect file-entry bytes before rewriting content. The first repair should preserve doctrine and remove only the entry defect.
- **Falsify / next test:** `python scripts/validate_skills.py` should pass after the mechanical repair, and `git diff` should show only BOM removal for the three affected skill files.

### Review packet - BOM repair, validator, CM-2 receipt

- **BOM fixes:** `.cursor/skills/dream/SKILL.md`, `.cursor/skills/fact-check/SKILL.md`, and `.cursor/skills/gate-review-pass/SKILL.md` were repaired mechanically. They are validation fixes, not doctrine edits.
- **Validator comparison:** `scripts/validate_skills.py` made the defect visible because `_parse_frontmatter` treats frontmatter as a strict file-start contract. That is useful: portable skill doctrine should fail loudly if its entry marker is hidden behind an encoding artifact.
- **CM-2 receipt comparison:** `artifacts/benchmarks/composition/2026-05-15/codex-gpt-5/cm-2-dopamine-flow-ai-creation/` records a composition-quality benchmark with verdict `Held`. That receipt tests expressive discipline and source-mode limits; it does not substitute for skill metadata validation.
- **Boundary:** mechanical validity, composition quality, and Record safety are separate checks. In this pass, skill metadata was verified, CM-2 was receipt-backed, and protected Record paths remained untouched.
- **Next wedge:** commit the three BOM repairs with the CM-2 benchmark receipt, or first add a regression test that writes a BOM-prefixed temporary skill and proves the current validator failure mode stays legible.

## Validation Snapshot

- `python scripts/validate_skills.py` passed after the BOM repair.
- CM-2 metadata JSON validated.
- Protected Record diff check was clean for `self.md`, `self-archive.md`, `recursion-gate.md`, `session-log.md`, and `bot/prompt.py`.

## Tomorrow Line

After the narrow durability commit, choose whether the next move is a tiny validator regression test for BOM-prefixed frontmatter or a compiled doctrine note for skill-entry hygiene.
