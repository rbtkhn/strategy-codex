# Named gaps (aligns with known-gaps.md).
version: 1
items:
  - id: BUILD-AI-GAP-001
    area: Handback provenance
    problem: Historical collapse of OpenClaw metadata at /stage
    status: closed
    related_integration_ids: []
    notes:
      - Resolved; provenance end-to-end into gate.

  - id: BUILD-AI-GAP-005
    area: Factorial scenario library
    problem: Matrix drift is CI-enforced; client-specific tail packs still outside repo baselines
    status: partial
    related_integration_ids: []
    notes:
      - variation-types.md; generate_scenarios.py factorial matrix (JSON v2 + markdown + --check FILE); handback_tail_stress.yaml V-01..V-08; pytest drift test on handback_tail_stress.matrix.md.

  - id: BUILD-AI-GAP-006
    area: Reasoning vs action (handback)
    problem: Optional staged_risk_tier vs narrative heuristics; broad NL understanding still manual
    status: partial
    related_integration_ids: []
    notes:
      - validate_handback_analysis.py constitution line vs meta; when staged_risk_tier is supplied, obvious two-way narrative/risk phrase mismatches fail validation; full semantic audit still out of scope.

  - id: BUILD-AI-GAP-007
    area: Progressive autonomy metrics
    problem: Dashboard + harness + CI smoke wired; operator habit + optional UI still limiters
    status: partial
    related_integration_ids: []
    notes:
      - log_shadow_decision.py appends JSONL; evaluate_autonomy_tiers.py loads tier_thresholds.yaml per --profile; pytest tests/test_autonomy_tiers.py.
      - Tests workflow runs evaluate_autonomy_tiers.py cold path (insufficient_data); build_dashboard Autonomy section; harness_warmup optional line.
