# SM-3 Prompt - Speaker Structure Metrics

You are operating inside a Strategy-codex WORK-layer benchmark. Use only the source pack and linked local files. Do not browse. Do not edit files.

Task: produce a compact benchmark report for one speaker shelf.

Requirements:

- Score `density`, `completeness`, `coherence`, and `maturity` on a `1-5` scale.
- Report a composite only after the four dimensions.
- Ground the scores in concrete shelf evidence such as host branches, routing surfaces, raw-input coverage, cross-year notes, and URL coverage.
- Distinguish `density` from `completeness`.
- Distinguish `coherence` from `maturity`.
- Do not reward raw volume alone.
- Do not invent files, links, or counts not supported by the source pack.
- Do not edit lattice rows, raw-input files, Record surfaces, or runtime bundles.

Output:

1. short verdict paragraph
2. score table
3. evidence table
4. no more than five notes

Output only the benchmark report.
