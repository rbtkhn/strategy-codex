# codex/ — moved

`codex/` has moved to [`../continuity/`](../continuity/).

Use `continuity/` for the durable chronology, accumulation, and notebook continuity layer.

Compatibility language:

- `strategy-codex` remains the project/public name.
- `codex/` is a legacy path.
- `memory.md` remains the rotatable session continuity buffer, not this folder.

See [`../docs/codex-to-continuity-rename.md`](../docs/codex-to-continuity-rename.md).
