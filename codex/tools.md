# strategy-codex tools

WORK only; not Record.

Use these local defaults in strategy-codex sessions:

- `rg`: [`../../../../.codex-tmp/rg.exe`](../../../../.codex-tmp/rg.exe)
- `python`: `C:\Users\rober\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`

Prefer the workspace-local `rg` copy when the WindowsApps binary is blocked, and prefer the bundled Python runtime when `python` or `python3` is not on PATH.
