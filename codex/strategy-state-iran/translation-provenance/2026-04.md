# Translation / republication — 2026-04 (index)
<!-- word_count: 211 -->

**Role:** **Track** how **text** and **attribution** **move** before they land in [weave/](../weave/) or [main `days.md`](../chapters/2026-04/days.md). One row per **bundle** or **line** you **cite** under uncertainty.

| Field | Notes |
|--------|--------|
| **id / anchor** | Short label (e.g. `araghchi-bundle-2026-04-20`) or `raw-input` path |
| **original language** | e.g. `fa`, `en`, `ar`, `mixed` |
| **official English available?** | `y` / `n` / `partial` |
| **translated by** | e.g. `office X in-post EN`, `operator`, `X machine gloss`, `third-party` |
| **repost chain depth** | `0` = primary; `1` = one repost; `2+` = chain (name nodes if known) |
| **modality** | `text` / `image` / `video` / `screenshot` |
| **paraphrase drift risk** | `low` / `medium` / `high` (headline vs long-form) |
| **cite confidence** | `low` / `medium` / `high` |
| **operator notes** | Optional — seam target, `thread:` to dual-register |

## Rows (add below)

| id / anchor | original language | official EN? | translated by | repost depth | modality | paraphrase risk | cite confidence | operator notes |
|-------------|-------------------|---------------|---------------|--------------|----------|-----------------|-----------------|----------------|
| *(example: wire new rows; April bundles already linked from legacy [iri-institutional thread](../voices/iri-institutional/thread.md).)* | | | | | | | | |

---

WORK only; not Record.
