# Weave (institutional ledger + interpretation)

Month-scoped synthesis surface for this state lane.

- `YYYY-MM-institutional-ledger.md` = what channels/signals said
- `YYYY-MM-institutional-weave.md` = what the constellation means

Chronology remains in `chapters/YYYY-MM/daily/`.

`weave/` may state the preferred lens on a braid, but the durable multi-arc object should usually live in `clusters/`. Shared note: [STATE-BRAID-MODEL.md](../../STATE-BRAID-MODEL.md).

WORK only; not Record.
