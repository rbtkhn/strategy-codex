# `greenwald` refined page — template (compat stub)

WORK only; not Record.

**Canonical scaffold:** [../../refined-page-template.md](../../refined-page-template.md).

When copying from the canonical template, substitute **`greenwald`** everywhere that file uses the literal token `{expert_id}` (preambles, filenames, `thread:` lines, appendix grep hints).

This stub keeps **`Template:`** lines in [`transcript.md`](transcript.md) and older links working.
