# Jiang surface orthogonality review - 2026-05

WORK only; not Record.

This review checks whether the current Jiang shelf is partitioned sharply enough to preserve cognitive depth, breadth, and connectivity without redundant surfaces.

## Scope

Surfaces reviewed:

- [jiang-profile.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-profile.md)
- [jiang-thread.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-thread.md)
- [jiang-transcript.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-transcript.md)
- [jiang-on-china.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-on-china.md)
- [jiang-vs-johnson-2026-05.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-vs-johnson-2026-05.md)
- [jiang-vs-johnson-others-2026-05.md](/C:/dev/strategy-codex/codex/speakers/jiang/jiang-vs-johnson-others-2026-05.md)

Bounded claim:

- this note audits the current Jiang shelf as it actually exists in May 2026
- it tests distinctness between continuity surfaces and comparison surfaces
- it does not try to redesign the whole Predictive History membrane in one pass

## Surfaces under review

- **continuity / routing surfaces:** `jiang-profile.md`, `jiang-thread.md`, `jiang-transcript.md`, `jiang-on-china.md`
- **comparison-only surfaces:** `jiang-vs-johnson-2026-05.md`, `jiang-vs-johnson-others-2026-05.md`

The main orthogonality pressure is not among the comparison notes. It is between the Jiang shelf's legacy `profile + thread + transcript` companion trio and the newer speaker-surface taxonomy where `thread` now means a canonical topical thread inside an arc.

## Arc audit

The Jiang shelf does not yet contain a mature person arc or relational arc in the newer sense. It remains a **profile-only membrane shelf with companion working files**.

### Arc distinctness table

| Surface | Owned continuity | Distinct frame | Distinct evidence habit | Distinct use | Status |
|---|---|---|---|---|---|
| `jiang-profile.md` | Jiang routing law and membrane boundary | routeable rationale | profile-level synthesis | first-open orientation | keep |
| `jiang-thread.md` | legacy distilled expert continuity | rolling strategy-expert thread format | machine plus journal layer | long-form distilled companion continuity | keep, but mark as legacy thread surface |
| `jiang-transcript.md` | recent verbatim Jiang-facing receipts | transcript retention | verbatim inbox-derived excerpts | recent source recall | keep |
| `jiang-on-china.md` | one bounded recurring question | routing note on China stance | local PH corpus comparison | answer "what is Jiang's China view?" quickly | keep |

### Arc findings

- `jiang-profile.md` and `jiang-on-china.md` are distinct enough: one owns route law, the other owns a bounded interpretive question.
- `jiang-transcript.md` is clearly distinct because it is a receipt surface.
- `jiang-thread.md` is useful, but its filename now risks collision with the newer topical-thread grammar. It behaves more like a **legacy distilled companion continuity file** than a canonical `<speaker>-thread-<topic>` surface.

## Thread audit

There are no canonical topical threads in the Jiang shelf yet.

The filename `jiang-thread.md` should therefore not be read as evidence that the shelf already contains a properly scoped topic thread under the new speaker taxonomy.

### Thread distinctness table

| Surface | Object | Mechanism | Source spine | Retrieval use | Status |
|---|---|---|---|---|---|
| `jiang-thread.md` | legacy expert continuity, not one bounded topic | monthly distillation plus machine extraction | Jiang lane transcript / inbox surfaces | companion continuity, not topical isolation | keep, but clarify role |

### Thread findings

- the shelf does **not** currently violate topical-thread orthogonality because it does not yet have multiple topical threads competing with one another
- the real issue is **taxonomy ambiguity**, not thread redundancy

## Redundancy findings

- `comparison note mistaken for continuity surface`
  - The two Jiang-vs-Johnson notes are correctly bounded comparison notes and should stay outside continuity ownership.
- `same source spine, different use`
  - `jiang-profile.md`, `jiang-thread.md`, and `jiang-transcript.md` all point back to the same Jiang lane, but they serve distinct routing, distillation, and receipt roles.
- `legacy naming pressure`
  - `jiang-thread.md` risks being misread as a canonical topical thread when it is really a legacy expert-thread companion file.

## Recommended actions

- `keep as is` for `jiang-profile.md`, `jiang-transcript.md`, `jiang-on-china.md`, and both Jiang-vs-Johnson notes
- `rename for clearer ownership` only if a later migration pass wants to normalize the legacy companion files
- `clarify role` now by explicitly labeling `jiang-thread.md` as a legacy distilled companion thread, not a canonical topical thread
- `do not create new topical threads` until one bounded recurring strand can defend distinct object, mechanism, and use

## Notebook use

Use this review when the notebook needs:

- a live example of orthogonality pressure inside a real speaker shelf
- a reminder that comparison notes should not be mistaken for continuity surfaces
- a warning that legacy filenames can create taxonomy confusion even when the actual functions remain distinct
- a basis for future Jiang shelf cleanup if the membrane becomes a fuller arc system

## Bottom line

The current Jiang shelf is **mostly orthogonal by use**, but not perfectly orthogonal by naming.

Its main defect is not redundancy of thought. It is that one legacy file name, `jiang-thread.md`, now sits uncomfortably inside a taxonomy where `thread` has become a more precise term. The right near-term fix is clarification, not collapse.
