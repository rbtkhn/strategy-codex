# Grace-Mar instance bundle (frozen archaeology)

Physical home for the embedded Grace-Mar **Record** and **bot** after root consolidation.

**Canonical Record files:** `self.md`, `recursion-gate.md`, `self-archive.md`, … — resolved via `scripts/repo_io.py` → `profile_dir()`.

**Subpaths:**

| Path | Role |
|------|------|
| `bot/` | Telegram / WeChat emulation (fork revive only) |
| `bootstrap/` | Legacy bootstrap docs |
| `recursion-gate-staging/` | Gate staging aids |

See [docs/grace-mar-instance-boundary.md](../../docs/grace-mar-instance-boundary.md) and [docs/root-directory-map.md](../../docs/root-directory-map.md).
