# MEMORY prune artifacts

Excerpts removed from `users/grace-mar/self-memory.md` by `scripts/prune_self_memory.py` land here (timestamped `*-prune.md`).

Optional: use `--archive` on the same script to also append the excerpt to `self-archive.md` § **IX. MEMORY PRUNE ARCHIVE**.

See `docs/memory-template.md` § *Lifespan & decay*.
