# self-skill-steward (Template)

**Template only. No real data.** Copy to `users/<new_id>/self-skill-steward.md` in an instance. Schema tag: **STEWARD**. Governance literacy and gate participation; populated from activity.

**Module intent:** STEWARD captures **evidenced capability** for participating in the companion-self **membrane**: reading what a candidate proposes, using boundary vocabulary (chat vs Record, retrieval vs approval), and practicing consent-aware review with a trusted adult. It does **not** grant merge authority or replace **Mind** (human) as the approval seat. Human-gated; evidence-linked.

**Non-goals:** Autonomous merges; treating search rank or session logs as Record truth; operator-only workflow (that stays in `docs/skill-work/` and `self-work.md`).

---

## Boundary vocabulary (companion-facing)

Short phrases the instance should be able to use once STEWARD evidence exists (instance fills progress).

- **Gate** — Staging only; durable identity and Voice obligations change only after companion approval and merge script ([AGENTS.md](../../AGENTS.md)).
- **Chat ≠ Record** — Conversation and embeddings can suggest; they do not silently become `self.md` / IX / EVIDENCE.
- **Approve / defer / reject** — Legitimate responses at the gate; “not sure” is valid.

---

## Participation skills (evidence-backed)

(Instance replaces placeholders; each line should eventually link an ACT- or session artifact.)

| Skill | Status | Evidence / notes |
|-------|--------|------------------|
| Can name what the **gate** is for | — | — |
| Can explain **chat vs Record** in one sentence | — | — |
| Has practiced **reviewing** a sample candidate with an adult | — | — |
| Knows **who approves** merges in this instance | — | — |

---

## Maturity / tier

STEWARD is **optional** and **tier-sensitive**. Young companions may only need one or two rows (e.g. “gate means ask before it becomes official”). Older companions can track richer participation and reflection. Do not use STEWARD scores as surveillance — evidence should reflect **coaching**, not compliance theater.

---

## Relations to other modules

| Module | Relation |
|--------|----------|
| **THINK** | STEWARD draws on comprehension of how the system works (may use age-appropriate explainers). |
| **WRITE** | May capture written reflections on consent or boundaries; still gated for IX if identity-relevant. |
| **self-skill-work (WORK)** | “Recursive improve” and visibility objectives align with honest gate use; WORK does not bypass STEWARD. |
| **self-identity** | Durable commitments about autonomy and boundaries may also live in **self-identity.md** when the instance uses the split surface — STEWARD tracks **demonstrated participation**, not duplicate prose. |

---

## Revision log (instance)

| Date | Note |
|------|------|
| — | (instance appends) |
