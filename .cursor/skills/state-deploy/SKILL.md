---
name: state-deploy
description: Archived statecraft deployment router. Use operator ship loop, transaction-router, or direct state-* lane openers for new work.
preferred_activation: state-deploy
activation: state-deploy
category: domain-pack
status: archived
scope_class: repo-governed
review_date: 2026-12-31
tags:
  - operator
  - archived
  - statecraft
---
# Archived — state-deploy

**Status:** Archived. Do not use for new work unless explicitly revived.

Deployment, publication, and ship decisions should route through the current operator ship loop, relevant runbook, or explicit statecraft transaction path.

## Legacy activation

When the operator says **`state-deploy`**, **`deploy statecraft`**, or Coffee **A Deploy**, run **one compressed ownership judgment** only:

1. authority → carrier → arena → maturity → depth
2. Ground in [`statecraft/README.md`](../../../statecraft/README.md), [`METHOD.md`](../../../statecraft/METHOD.md), and [`transaction-router.md`](../../../statecraft/sheets/transaction-router.md)
3. Hand to the owning **`state-*`** lane (`state-america`, `state-china`, `state-persia`, `state-russia`) or a named comparison path

Do **not** run this archived skill body or restate full lane doctrine.

## No active invocation

If this method is needed again, revive it through:

```text
skills/skill-candidates.md
→ skills/_drafts/<name>/SKILL.md
→ tested in real use
→ promoted back to active status
```

## No independent methodology

Do not extend this archived file. If current work needs deployment procedure, create or update an active runbook instead.
