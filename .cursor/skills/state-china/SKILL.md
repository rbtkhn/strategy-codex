---
name: state-china
description: Open the China statecraft lane directly. Use when the operator says state-china, China state skill, or wants China to carry a statecraft object through order, continuity, anti-disorder legitimacy, administrative carry, route stability, or quiet leverage.
preferred_activation: state-china
activation: state-china
category: domain-pack
status: active
scope_class: repo-governed
---
# State China

`state-china` is the primary China lane machine. It is `README`-first and `helix`-default.

Open this lane when the live object turns on continuity, route stability, administrative carry, anti-disorder legitimacy, or how China preserves leverage without inheriting visible chaos.

Short doctrine: `state-deploy` decides the lane. `state-china` decides what China can legitimately draft, carry, accept, reject, and institutionalize.

## Identity Block

China carries order, continuity, anti-disorder legitimacy, administrative carry, and quiet leverage.

Core test: what preserves leverage without forcing China to own visible disorder?

## Boundary

- WORK only; not Record.
- Start at the lane front door before descending.
- Use the literature lens as a subordinate civilization-layer test, not as a separate top-level mode.
- Do not directly rewrite lane books from live drafting.

## Constitutional Split

`state-china` is a substantive lane skill, not a routing skill.

- Assume China already owns the object unless the operator explicitly asks for deployment or comparison.
- Decide what China can legitimately draft, carry, accept, reject, and institutionalize.
- If the object is obviously cross-lane or not honestly China-owned, say so and hand back toward `state-deploy` or a comparison path.

Once lane ownership is clear, identify the governing pair within the **Civilizational Statecraft Framework** before descending. If the pair is not already obvious, run `statecraft-framework` first.

## Source Surfaces

Open in this order unless the request is already narrower:

1. `statecraft/china/README.md`
2. `statecraft/china/helix.md`

Descend only with a named reason:

- `statecraft/china/civilization/objects/state-memory.md` for continuity, face, anti-humiliation authority, and development legitimacy
- `statecraft/china/civilization/geo.md` for route exposure and settlement geometry
- `statecraft/china/civilization/war.md` for coercive classification and escalation boundaries
- `statecraft/china/civilization/peace.md` for stabilization formulas, review, and bounded equilibrium
- `statecraft/china/empire/seed-instruments.md` for outward leverage, finance, shipping, insurance, and control stack
- `statecraft/china/state/` for present carriers and implementation burden
- `statecraft/china/transactions/` for draftable instruments
- `statecraft/china/civilization/lit.md` for harmony, face, hierarchy, patience, or clause-language stress tests

## Governing-Pair Rule

After ownership is clear, force this diagnosis immediately:

- what governing pair is load-bearing: `civilization / empire`, `faith / science`, or `memory / desire`?
- what dominant layer inside that pair is actually governing the object?

Compact `faith / science` rule:

- treat sacred or moral legitimacy and procedural or technical competence as separate truth-orders
- do not collapse sacred authorization into technical competence
- do not collapse technical competence into sacred legitimacy

## Routing Rule

- Stay at `README.md` when the operator still needs lane orientation.
- Once China is clearly the lane, diagnose the governing pair immediately.
- Descend to `helix.md` by default after the governing pair is named.
- Descend further only when the object is narrower and name the reason briefly in governing-pair terms.
- If the governing pair still feels smoother than the retrieval consequences, run `statecraft-framework` before narrower descent.

## Subordinate Families

Use subordinate families only after the governing pair is named:

- harmony / disorder testing
- face / hierarchy testing
- strategic patience testing
- implementation-language testing
- clause-language stress tests for whether order is preserved without humiliation

`lit` remains subordinate. `geo / war / peace` are also subordinate retrieval-and-expression families. None of them should be treated as alternate top-level modes.

## Default Output

When invoked without a named object, output exactly this shape:

```markdown
**China Statecraft**
- Identity test:
- Governing question:
- Governing pair:
- Dominant layer:
- Best next move:
- Default descent:

**China Menu - reply A-D**
A. Order / flow diagnosis
B. Administrative-carrier check
C. Anti-disorder instrument draft
D. Market or corridor stability test
```

When invoked with a named object, make the A-D options topic-specific.

When invoked with a named object, also name the governing pair and dominant layer before offering subordinate-family descent.

## Recursive-Update Membrane

If live drafting exposes a durable Chinese pattern, carrier, threshold, or precedent, stage a reviewable candidate in `statecraft/china/updates/pending.md`.

Do not directly rewrite:

- `helix.md`
- `civilization/`
- `empire/`
- `state/`
- `transactions/`
