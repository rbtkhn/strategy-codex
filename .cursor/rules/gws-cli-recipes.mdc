---
description: Optional Google Workspace CLI pointers â€” attach when using gws; not always on
alwaysApply: false
---

# GWS CLI (optional)

Operator-only continuity tooling. **Not** Record truth; **not** a replacement for `harness_warmup.py` or the gate.

- **Full runbook:** `docs/archive/skill-work-legacy/work-dev/google-workspace-cli-operator.md`
- **Config:** local `.env.gws.local` (see `.gitignore` â€” `.env.*.local` is ignored).
- **Logging:** use a local work-dev log file; do **not** treat `session-log.md` as an operator tooling audit trail.

When the operator says to use GWS, follow that docâ€™s guardrails (read-only first, no secrets in git).

