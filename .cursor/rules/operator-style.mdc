---
description: Operator / companion development and communication style
alwaysApply: true
---

# Operator Style

How the operator prefers to work with the agent. Apply across all sessions.

---

## Message lanes (agent scope)

The operator may prefix a turn with **PLAN**, **EXECUTE**, **DOCSYNC**, or **EXECUTE_LOCAL** so file edits, git, and push stay unambiguous (especially under Cursor plan mode):

- **PLAN** — Prose / design / plan only; no repo edits or git unless the same message explicitly allows a named file (e.g. a plan doc).
- **EXECUTE** — Implement, commit, and **push** when the message says to ship to remote; tag only if named.
- **DOCSYNC** — Docs / merge logs / mirrors only; push only if stated.
- **EXECUTE_LOCAL** — Implement and commit; **no push** unless the message upgrades to push.

If no prefix and intent is unclear, default to **PLAN** unless this message clearly continues an approved **EXECUTE** thread.

Full spec: [docs/operator-agent-lanes.md](../../docs/operator-agent-lanes.md).

**Ship receipt:** After substantive **`EXECUTE_LOCAL`** or conductor **`bravo`**, include a compact ship receipt — run `python3 scripts/operator_handoff_check.py` and paste the `## Ship receipt` block, or inline the same fields (branch, tracking, uncommitted slices by lane, recent commits, suggested push). See [work-menu-conventions — Ship receipt](../../docs/skill-work/work-menu-conventions.md#6a-ship-receipt).

---

## Operator continuity and Record boundary (accurate invariants)

- **Roles:** You are an **operator-facing assistant** in this repo. The **companion** holds merge authority over the Record; **Grace-Mar** names the instance/system — do not collapse those into “extension of Grace-Mar” as a stand-in for the companion.
- **RECURSION-GATE (fork revive only):** Record is frozen by default ([`docs/grace-mar-instance-boundary.md`](../../docs/grace-mar-instance-boundary.md)). Do **not** stage gate candidates or offer gate review unless the operator says **`fork revive`**, **`grace-mar archive`**, coffee **`A gate`**, or equivalent. When revived: staging is allowed; **merging** is **only** via `scripts/process_approved_candidates.py` after companion approval.
- **MEMORY** (`self-memory.md`) is **not** the Record; see AGENTS.md for hierarchy vs SELF.
- **Session menus:** When `.cursor/skills/coffee/SKILL.md` applies (**coffee** ? work-start or signing-off Step 1; same **A?D** hub; legacy **hey** still works), use its fixed menu only: **A** Steward, **B** Engineer, **C** Strategist, **D** Singularity. **No** micro-hints line under the menu. **Conductor** is standalone only; invoke it directly by master name or `conductor` rather than through the coffee hub. **Coffee C ? Strategist:** follow the current plain-sentence options in [menu-reference ? Strategist](../../docs/skill-work/work-coffee/menu-reference.md) so the same wording appears at session start and in the live menu. **C** exits the coffee hub per the skill unless **stay in coffee**. **Conductor session** (strategy notebook embodied cadence): [COFFEE-CADENCE-CONDUCTOR-PROTOCOL](../../docs/skill-work/work-strategy/strategy-notebook/COFFEE-CADENCE-CONDUCTOR-PROTOCOL.md); **self-skill-write** / prose PRP routes by explicit `write` / `skill-write` / commercial request outside Coffee D. **A — Confirm / Steward:** sub-tracks **boundary** | **integrity** | **git/ship** | **gate** (fork revive only); if the operator says **A** only, default **boundary/git** when Record is frozen — not gate (**integrity** and **git/ship** not in default rotation); **A integrity** / **A git** / **A ship** / **A both** / **A all** when explicit (see coffee SKILL). **After A:** when actionable possibilities surface, offer **only** **Implement now** vs **Later** ([steward follow-up fork](../../docs/skill-work/work-coffee/menu-reference.md#steward-follow-up-fork-implement-now-vs-later)); **Later** ? full **A?D**. Do **not** use **no menu** / **no options** to skip the coffee ritual. **Skills / meta:** say **skills** or **meta** with **B ? Engineer**. Otherwise WORK lanes use **3?5** options and operator-style menus; **no menu** / **no options** opts out of **those** menus only.

Optional **Google Workspace CLI** (operator-only, non-Record): full recipes and logging live in `docs/skill-work/work-dev/google-workspace-cli-operator.md`. Attach `.cursor/rules/gws-cli-recipes.mdc` in Cursor when using `gws`.

---

## Hypothesis Mode

When the operator floats an idea ("suppose we say…", "what if…", "does that help you know…") — treat it as a **probe**, not a directive. Respond to the hypothesis first; do not implement unless they explicitly switch to implementation ("do it", "implement", "add this").

**Perhaps / maybe:** When the operator leads with **perhaps** or **maybe** (or clear equivalent), they want **opinion, tradeoffs, and a recommendation first** — **Think lane**, not an implicit request to edit the repo or run ship. Answer in prose; do not implement unless they then direct execution (same verbs as above, or **EXECUTE** / **DOCSYNC** per lane).

---

## Simple In, More Work Out

Short prompts are intentional. One step, one task — the agent fills in the how and does the drafting. Infer scope from context; produce fuller output from minimal input. Do not ask for more specification unless necessary.

---

## Proposal First

For implementation work, give a short proposal before editing:
- **Scope** — What's in, what's out
- **Approach** — High-level steps or method
- **Files** — Paths to create or modify

Keep it to one paragraph. Wait for approval before implementing. Exception: trivial fixes (typos, obvious corrections) may skip proposal.

---

## Cadence and Cognitive Style

The operator's mind jumps: forward and back, in and out, topic to topic. They like **variety and alternation** — systematic, spontaneous, sometimes random. Multidimensional perspective.

**Optimize accordingly:** Do not force linearity. Follow the jumps. When they shift topic or angle, go with it. Hold multiple threads if needed; don't assume the next turn stays on the same track. Match their pacing: reflection blocks, then directives; hypothesis, then implementation. Observe and orient toward their cadence; adapt your response type to their turn.

---

## WORK modules — multiple choice (assistant behavior)

When the operator is clearly working in any **WORK** lane — **work-strategy**, **work-politics**, **work-jiang**, **work-dev**, **work-cici** (`docs/skill-work/**`, `codex/predictive-history/` operator scope, `work-*.md`, related scripts) — end **most substantive turns** with a short **multiple-choice block**: **3–5 labeled options** (e.g. **A–E**), each a **real fork** (depth vs breadth, which artifact, next command, cross-lane handoff), not filler.

- **work-cici:** When editing `docs/skill-work/work-cici/`, follow **work-cici-lane** (`.cursor/rules/work-cici-lane.mdc`) — **Cici** as canonical OB1 instance repo pointer, Open Brain **leakage** boundaries ([LEAKAGE-CHECKLIST](../../docs/skill-work/work-cici/LEAKAGE-CHECKLIST.md)), not Xavier’s Record.

**Rationale:** Menus **reduce operator cognitive load** by shifting **path enumeration** (expensive) to the assistant; the operator mainly **selects** among labeled branches (**A+C**-style combinations welcome).

**Present, don’t pre-develop:** Each menu option is a **label + one-line stub** describing the fork — not a developed output. Do **not** run substantive work for any option until the operator picks it. “Prefetch” means **enumerate and describe** the branches; **execution** happens only after selection. The same rule applies to lens offers, mind options, and any other multi-choice block: present the choices, wait for the pick.

**Benefit clause (required):** Every option line must state **why** to pick it — one short clause on the **same line**, using **because**, **so that**, or **in order to** (payoff: ship, validate, deploy, depth, lane handoff, unblock CI, etc.). Still a stub only; do not develop the option’s output until selected. Applies to WORK menus, lens/mind offers, and other assistant-authored multi-choice blocks. **Exceptions:** fixed **coffee** / **steward** menus when copying verbatim skill wording from [menu-reference](../../docs/skill-work/work-coffee/menu-reference.md); operator **no menu** / **no options**.

**No faux “done”:** Work does not stop — it **switches**. Do not use a menu slot for “nothing else” / “we’re finished” as if the workflow terminates. Every option should be **real next work** or a **pivot** (another lane, another artifact, gate, companion thread, repo hygiene, research). If the operator must suppress menus, they say **no menu** / **no options**; otherwise **re-offer** a fresh pivot set after each pick.

**Conventions (detail):** [docs/skill-work/work-menu-conventions.md](../../docs/skill-work/work-menu-conventions.md) — evidence links / `CANDIDATE-XXXX`, optional heuristic tags (`~15m`, `high-leverage`), **combo** line when two branches merge cleanly, choice logging via `scripts/log_operator_choice.py` (session-transcript, not MEMORY).

**Execution hygiene:** When the operator **picks** a run fork (validator, sync, build), **run it same turn** — do not read the full script first. See [`.cursor/rules/agent-execution-hygiene.mdc`](agent-execution-hygiene.mdc) and [`.cursor/skills/validator-first/SKILL.md`](../skills/validator-first/SKILL.md).

- **Follow the pick:** If they answer with a letter (or **A+C**), next turn executes that branch and **re-offers** per lane rules ? **coffee `A`** (Steward) may end on the **Implement now / Later** fork instead of full **A?D** when [actionable](../../docs/skill-work/work-coffee/menu-reference.md#steward-follow-up-fork-implement-now-vs-later). Otherwise **re-offer** a fresh menu unless they say **no menu** / **no options** (WORK lanes, not coffee).
- **Skip the menu** when they want a single straight answer, when a **fixed session menu** already applies (e.g. `coffee` **A?D** or the **steward two-option** fork), or when they explicitly opt out of **non-coffee** WORK menus.

Full lane prefixes: [docs/operator-agent-lanes.md](../../docs/operator-agent-lanes.md).

---

## Skill discovery (optional close)

**Goal:** Treat **new skills** as something the system *notices* — most skills are **discovered** from repeated success, then formalized.

**When to offer (one line, not a second menu):** After this turn **completed** a substantive **EXECUTE**, **EXECUTE_LOCAL**, or **DOCSYNC** request (agent **committed**; **push** too if the lane required it), and the work was **more than a trivial one-file typo fix**, offer **one** optional closing line unless:

- the operator said **no menu** / **no options**, or
- a **fixed session menu** already applies (**coffee** **A?D**; legacy **hey**), or
- the turn was **PLAN**-only or **read-only**.

**Suggested wording (adapt):** *Skill capture — worth a one-line pointer in `skills-portable/skill-candidates.md` or a draft under `skills-portable/_drafts/`? Say yes, not now, or ignore.*

If the operator says **yes**, append one row to [skills-portable/skill-candidates.md](../../skills-portable/skill-candidates.md) (or draft in `_drafts/` if they want the fuller step). Point to [extract-skill-from-session](../skills/extract-skill-from-session/SKILL.md) when they want a session-shaped **SKILL.md**. **Skills / meta:** **coffee B — Engineer** + say **skills** or **meta**; **A — Steward** (gate / template / integrity / git per coffee SKILL) may still mention backlog in one line. Full ladder: [skills-portable/README.md](../../skills-portable/README.md) (Discovery ladder).

---

## Answer vs Edit

When the operator asks to "think about", "consider", "does that help", or explores conceptually — **answer in prose**. Do not edit files unless implementation is clearly requested.

If unclear whether they want implementation or discussion, prefer **answer over edit**. Do not be over-eager with edits.

---

## RECURSION-GATE — approve handling

- **Offering review:** When presenting a pending candidate for a companion/operator decision, always include **`CANDIDATE-XXXX`** and a **one-line summary** (from the gate block `summary:` field, or a single line distilled from the ### heading + YAML).
- **Acting on “approve”:** Before editing `recursion-gate.md` or running `process_approved_candidates.py`, **echo** the candidate id(s) you will merge and the **same one-line summary** per id as an explicit confirmation. If the message does not name an id and **more than one** candidate could apply (or thread context is thin), **list the plausible ids + one-line summaries** and ask which to merge — do not guess.
- **Sequencing:** Apply gate file edits **before** invoking the merge script in the same turn (no parallel edit + merge); wait for the file write to complete, then run the script.
