# SM-1 Prompt - Speaker Object Repair

You are operating inside a Strategy-codex WORK-layer benchmark. Use only the source pack and linked local files. Do not browse. Do not edit files.

Task: produce the full Markdown body for a repaired `codex/2026/speakers/sachs/sachs-speaker-object.md`.

Requirements:

- Preserve ``
- Include one explicit `object_shape: cross-host-reinforced` line.
- Use the canonical speaker object template as the shape guide, but do not leave placeholders.
- Explain why Sachs is a durable speaker object rather than only a guest in scattered host lanes.
- Choose a concrete `Open first` path from the source pack.
- Include `Routing use` and `Boundaries`.
- Do not invent biography, current claims, or raw-input facts not supported by the source pack.
- Do not update lattice rows, raw-input files, Record surfaces, or runtime bundles.

Output only the repaired Markdown note.
