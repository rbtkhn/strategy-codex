# Speaker Memory Benchmark Demo Summary

**Status:** demonstration run. non-authoritative. Not Record. Not EVIDENCE.

This demo ran both first speaker-memory fixtures without editing source speaker folders.

## Results

| Benchmark | Target | Closeout | Main demonstration |
|---|---|---|---|
| `sm-1-speaker-object-repair` | Sachs speaker object | `Held` | Forces object-shape, open-first, routing, and overclaim boundaries. |
| `sm-2-speaker-arc-ranking` | Diesen x Freeman arc | `Held` | Forces host-local ranking, paired-read judgment, and lattice restraint. |

## What This Proves

The benchmarks are useful because they separate three things that can otherwise blur:

- fluent prose versus durable memory architecture
- speaker identity versus host-conditioned arc form
- lattice lookup versus interpretation surface

The demo also shows how a benchmark can reveal actionable hygiene without touching canonical files: SM-2 noticed that the current Diesen x Freeman arc is substantively good but lacks an explicit `` boundary line.

## Next Useful Benchmark Test

Run a deliberately weak model output against each rubric. The best stress cases would be:

- SM-1: a polished Sachs biography with no object shape or open-first route
- SM-2: a Freeman arc ranked by recency, with lattice interpretation and no host-form distinction

Those weak cases should close `Broke` or `Weakened`; if they do not, the rubrics need tightening.
