# Workflow depth audit artifacts

Generated JSON (and optional Markdown) from `scripts/build_workflow_depth_report.py` / `scripts/build_workflow_depth_hints.py`.

- **Inspection only** — not Record truth, not governance.
- Safe to commit when used as demos; default batch output targets this directory.

See `docs/runtime/workflow-depth-audit.md`.
