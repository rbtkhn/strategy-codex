# Shadow merge reports (optional output)

Operator-local **Markdown** previews from [`scripts/runtime/shadow_merge_simulator.py`](../../scripts/runtime/shadow_merge_simulator.py) (`-o` / `--output`). **Not** canonical Record truth and **not** merge receipts.

See [docs/orchestration/shadow-merge-simulator.md](../../docs/orchestration/shadow-merge-simulator.md).

**Git:** Generated `*.md` reports stay **gitignored**; this **README** and `.gitkeep` are tracked so the folder and doc pointer ship with the repo.
