# Conductor evaluation artifacts (derived)

**WORK / observability only** — not Record, not gate authority.

Outputs generated by [`scripts/run_conductor_eval_harness.py`](../../../scripts/run_conductor_eval_harness.py), optionally consumed manually or in CI.

- Schema: [`schemas/registry/conductor-session-metrics.v1.json`](../../../schemas/registry/conductor-session-metrics.v1.json)
- Interpretation: [`docs/skill-work/work-coffee/conductor-observability.md`](../../../docs/skill-work/work-coffee/conductor-observability.md)

Default: `*.json` here is gitignored (see `.gitignore`). Commit **fixtures** under `tests/fixtures/conductor/` instead of operator-generated snapshots unless you intentionally snapshot.
