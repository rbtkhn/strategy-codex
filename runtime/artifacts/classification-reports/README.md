# Classification risk reports (optional output)

Operator-local **Markdown** reports from [`scripts/runtime/surface_misclassification_detector.py`](../../scripts/runtime/surface_misclassification_detector.py) (`-o` / default under this folder). **Advisory only** — not canonical Record truth.

See [docs/orchestration/surface-misclassification-detector.md](../../docs/orchestration/surface-misclassification-detector.md).

**Default:** `*.md` here are **gitignored** (see repo `.gitignore`); `.gitkeep` preserves the folder.
